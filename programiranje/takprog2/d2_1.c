#include <stdio.h>

int main(void)
{
    int n, m;
    scanf("%d %d", n, m);

    unsigned int np[m];
    int izn[m];
    int tocak[n];
    int tocakb[n];

    for(i = 0; i < m; i++)
        scnaf("%d %d", &np[i], &izn[i]);

    return 0;
}
